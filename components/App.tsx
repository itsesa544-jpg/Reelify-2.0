import React from 'react';

// This file is a placeholder to prevent naming conflicts.
// The main App component is located at src/App.tsx (root directory).

const AppPlaceholder: React.FC = () => {
  return null;
};

export default AppPlaceholder;